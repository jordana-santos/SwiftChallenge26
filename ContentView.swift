import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image("teste")
                .imageScale(.large)
            Text("Hello, world!")
            
        }
    }
}
